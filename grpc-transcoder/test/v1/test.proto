syntax = "proto3";

package test.v1;
option go_package = "testv1";

service TestService {
	rpc Get(TestRequest) returns (TestResponse) {
	};
}

message TestRequest {
	string id = 1;
	bytes body = 2;
}

message TestResponse {
	string id = 1;
	bytes body = 2;
}
